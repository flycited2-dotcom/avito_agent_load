feed_out/
# только рантайм-каталог в корне; НЕ src/avito_bridge/state/ (однажды из-за этого потеряли модуль)
/state/
/runtime/
/data/
# Supplier workbooks are operational inputs, never source or test fixtures.
*.xls
*.xlsx
.venv/
.worktrees/
__pycache__/
*.pyc
.coverage
.coverage.*
.pytest_cache/
.ruff_cache/
coverage.xml
coverage.json
coverage-current.json
htmlcov/
.env
avito_err*.json
avito_*.json
xlsx_*.txt
xlsx_*.json
*.txt
*.json
!**/tests/**
# Контент и конфиг каталога — отслеживаем несмотря на широкие *.txt/*.json выше
!avito-descriptions/
!avito-descriptions/*.txt
!avito-descriptions/*.json
!config/
!config/*.yaml
!config/card_modes.json
# !**/tests/** выше случайно возвращал в трекинг кэш байткода — исключаем обратно
tests/__pycache__/
*.egg-info/
